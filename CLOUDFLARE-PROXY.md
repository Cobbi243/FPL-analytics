# Власний проксі через Cloudflare Worker (5 хвилин)

Навіщо: публічні CORS-проксі ненадійні — то ріжуть великі файли, то блокують FIFA, то взагалі лежать. Твій власний Worker працює завжди, безкоштовно, без лімітів для твого масштабу. Усе робиться в браузері, без терміналу.

---

## Крок 1. Зареєструйся на Cloudflare
1. Зайди на https://dash.cloudflare.com/sign-up
2. Створи безкоштовний акаунт (email + пароль), підтверди email

## Крок 2. Створи Worker
1. У лівому меню: **Workers & Pages**
2. Натисни **Create application** → **Create Worker**
3. Дай назву, напр. `fpl-proxy` (вона буде в адресі)
4. Натисни **Deploy** (поки що з шаблонним кодом)

## Крок 3. Встав свій код
1. Після деплою натисни **Edit code** (або **Quick edit**)
2. Виділи весь шаблонний код у редакторі (Ctrl+A) і видали
3. Відкрий файл `cloudflare-worker.js` (з цієї папки) у Блокноті, скопіюй увесь вміст
4. Встав у редактор Cloudflare
5. Натисни **Deploy** / **Save and deploy**

## Крок 4. Скопіюй адресу воркера
Після деплою побачиш адресу типу:
```
https://fpl-proxy.твій-нік.workers.dev
```
Скопіюй її.

## Крок 5. Встав адресу в додаток
1. Відкрий свій сайт (FPL дашборд)
2. Перейди на вкладку, яка не вантажиться (напр. WC 2026) — побачиш екран помилки
3. У полі **"Власний проксі"** встав адресу воркера, додавши в кінці `/?url=`:
   ```
   https://fpl-proxy.твій-нік.workers.dev/?url=
   ```
4. Натисни **"Спробувати знову"**

Готово — тепер дані тягнуться через твій надійний проксі. Адреса збережеться в браузері, вводити щоразу не треба.

---

## Важливо
- Адреса проксі зберігається **локально в твоєму браузері**. Якщо ділишся сайтом з друзями — кожному треба буде вставити її окремо (або скажи мені, і я вшию її прямо в код сайту, щоб працювало в усіх із коробки).
- Безкоштовний тариф Cloudflare Workers — 100 000 запитів на день. Тобі й друзям цього з головою вистачить.
- Воркер налаштований пускати тільки до `fantasy.premierleague.com` і `play.fifa.com` — щоб ніхто сторонній не зміг гоняти через нього довільні запити.

## Якщо хочеш вшити проксі в код (щоб у всіх працювало автоматично)
Скажи мені адресу свого воркера — я пропишу її в `index.html` як проксі за замовчуванням. Тоді ні тобі, ні друзям не треба нічого вставляти вручну, сайт працюватиме одразу.
